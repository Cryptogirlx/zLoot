const { BigNumber } = require("@ethersproject/bignumber");
const { ethers } = require("hardhat");

export const constants = {
    NFT: {
        tokenId1: 1,
        tokenId2:2,
        totalSupply:10000
    }
}
